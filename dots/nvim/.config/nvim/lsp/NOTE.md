# NOTE

We could manually configure the LSPs in each file, but that's kind of tedious.

For this, I'll be using nvim-lspconfig, which provides a set of default LSPs that I'll use instead of configuring each LSP's default values on my own.
