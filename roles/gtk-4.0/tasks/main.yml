---
- name: Create the '$HOME/.config/gtk-4.0' directory if not exists
  ansible.builtin.file:
    path: "{{ ansible_facts['user_dir'] }}/.config/gtk-4.0"
    mode: "0755"
    state: directory

- name: Create symlink of files to '$HOME/.config/gtk-4.0' directory
  ansible.builtin.file:
    src: "{{ role_path }}/files"
    dest: "{{ ansible_facts['user_dir'] }}/.config/gtk-4.0"
    state: link
    force: true
