---
- name: Create the '$HOME/.config/kitty' directory if not exists
  ansible.builtin.file:
    path: "{{ ansible_facts['user_dir'] }}/.config/kitty"
    mode: "0755"
    state: directory

- name: Create symlink of files to '$HOME/.config/kitty' directory
  ansible.builtin.file:
    src: "{{ role_path }}/files"
    dest: "{{ ansible_facts['user_dir'] }}/.config/kitty"
    state: link
    force: true
